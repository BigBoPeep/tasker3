export const DEFAULT_SETTINGS = {
  filters: { completed: true, incomplete: true, overdue: true },
  sortBy: "deadline",
  sortOrder: "descending",
};

export const STORE_NAMES = {
  tasks: "tasks",
  projects: "projects",
  workspaces: "workspaces",
  settings: "settings",
};
